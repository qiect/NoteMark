# 架构设计

## 概述

NoteMark 采用分层模块化架构，由 6 个项目组成，各项目职责明确，依赖关系清晰。核心设计思路是将 Markdown 处理、OneNote 交互、渲染服务和主题管理解耦，通过 OneNoteAddIn 项目作为宿主进行协调。

## 目标框架

| 项目 | 目标框架 | 说明 |
|------|----------|------|
| OneNoteAddIn | .NET Framework 4.8 | COM Add-In 宿主，需要与 OneNote COM 互操作 |
| MarkdownEngine | .NET Standard 2.0 | 纯逻辑，无平台依赖 |
| OneNoteConverter | .NET Standard 2.0 | XML/Markdown 转换逻辑，OneNote API 调用已移至 AddIn |
| RenderingServices | .NET Standard 2.0 | 代码高亮和模板生成，WebView2 渲染已移至 AddIn |
| ImportExport | .NET Standard 2.0 | 文件和剪贴板操作 |
| ThemeManager | .NET Standard 2.0 | CSS 主题管理，无外部依赖 |

选择 .NET Framework 4.8 作为 AddIn 宿主框架的原因：

- OneNote COM Add-In 需要通过 RegAsm.exe 注册，.NET Framework 4.8 原生支持
- .NET 8 + COM Host 方案在 OneNote 中无法正常加载（已验证）
- .NET Framework 4.8 可以引用 .NET Standard 2.0 项目，业务层代码无需修改

## 模块依赖关系

```
┌─────────────────────────────────────────────────────────┐
│                     OneNoteAddIn                        │
│          (.NET Framework 4.8 / COM Add-In)              │
│     IDTExtensibility2 + IRibbonExtensibility            │
│     OneNoteApiWrapper / WebView2Helper                  │
│     DiagramRenderService / LatexRenderService           │
├─────────┬──────────┬──────────┬──────────┬─────────────┤
│         │          │          │          │             │
│         ▼          ▼          ▼          ▼             │
│  MarkdownEngine  OneNoteConverter  RenderingServices   │
│                  ImportExport       ThemeManager        │
│         ▲          ▲                    ▲              │
│         │          │                    │              │
│         │    ┌─────┘                    │              │
│         │    │                          │              │
└─────────┼────┼──────────────────────────┼──────────────┘
          │    │                          │
          ▼    ▼                          │
   MarkdownEngine ◄──────────────────────┘
```

## 项目详解

### OneNoteAddIn

COM Add-In 入口项目，负责与 OneNote 进程集成。

| 类 | 职责 |
|---|------|
| `NoteMarkAddIn` | Add-In 主入口，实现 `IDTExtensibility2` 和 `IRibbonExtensibility`，ProgId 为 `NoteMark.AddIn`，使用 `ClassInterfaceType.None`，Lazy 延迟初始化 |
| `MarkdownRenderHandler` | Markdown 渲染处理器，处理实时渲染、F5 渲染和源码模式切换 |
| `ExportHandler` | 导入导出处理器，处理 F8 剪贴板导出、文件导入和文件导出 |
| `OneNoteEventHandler` | OneNote 事件处理器，监听页面切换事件 |
| `KeyboardHook` | 全局键盘钩子，拦截快捷键并分发事件（首次按钮点击时才安装） |
| `AddInSettings` | 插件设置管理（单例），持久化到 JSON 文件 |
| `AppLogger` | 异步日志记录器（单例），队列缓冲 + 定时刷新 |
| `NoteMarkRibbon` | Ribbon UI 自定义，定义按钮和动态主题菜单 |
| `OneNoteApiWrapper` | OneNote COM API 封装，提供页面内容读写和层级查询 |
| `OneNotePageUpdater` | 页面更新器，支持替换选中文本、追加内容和替换整页 |
| `WebView2Helper` | WebView2 辅助类，封装控件创建、脚本执行、截图和消息通信 |
| `DiagramRenderService` | 图表渲染服务，通过 WebView2 渲染 Mermaid 等图表 |
| `LatexRenderService` | LaTeX 渲染服务，通过 WebView2 + KaTeX 将公式渲染为图片或文本 |

**依赖**: MarkdownEngine, RenderingServices, ThemeManager, OneNoteConverter, ImportExport, Interop.Microsoft.Office.Interop.OneNote, Microsoft.Web.WebView2

**注册方式**: 使用 `RegAsm.exe` 注册 COM 组件，不再使用 `EnableComHosting` / `comhost.dll`

### MarkdownEngine

Markdown 解析和渲染引擎，是整个系统的核心模块。

| 类 | 职责 |
|---|------|
| `MarkdownParser` | 解析入口，配置 Markdig 管道并调用自定义扩展 |
| `MarkdownDocument` | 文档模型，包含从 Markdig AST 到自定义元素模型的转换逻辑 |
| `MarkdownRenderer` | 渲染器基类，包含 `HtmlMarkdownRenderer`（HTML 输出）和 `OneNoteXmlRenderer`（OneNote XML 输出） |
| `MarkdownExtension` | Markdig 扩展集合，包含数学公式、图表和引用块标题图标三个扩展 |
| `MarkdownImage` | 图片数据模型 |

**元素模型**:

```
BlockElement (抽象基类)
├── ParagraphElement       段落
├── HeadingElement         标题 (h1-h6)
├── CodeBlockElement       代码块
├── DiagramBlockElement    图表块 (Mermaid/Flow/Sequence/Mindmap)
├── QuoteBlockElement      引用块
├── TableElement           表格
├── ListElement            有序/无序列表
├── TaskListElement        任务列表
├── MathBlockElement       数学公式块
└── HorizontalRuleElement  水平线

InlineElement (抽象基类)
├── TextElement            纯文本
├── BoldElement            加粗
├── ItalicElement          斜体
├── StrikethroughElement   删除线
├── CodeInlineElement      行内代码
├── LinkElement            链接
├── ImageElement           图片
└── MathInlineElement      行内数学公式
```

**依赖**: Markdig (NuGet)

### OneNoteConverter

OneNote XML 与 Markdown 的双向转换层。

| 类 | 职责 |
|---|------|
| `OneNoteXmlConverter` | 转换协调器，整合 HTML 转换器和 Markdown 转换器，支持双向转换和主题应用 |
| `HtmlToOneNoteConverter` | HTML → OneNote XML 转换器，处理标题、段落、列表、表格、引用、代码、图片等元素 |
| `OneNoteToMarkdownConverter` | OneNote XML → Markdown 转换器，解析 OneNote 页面结构并提取样式信息 |
| `OneNoteXmlBuilder` | OneNote XML 构建器，生成页面、大纲、表格、图片等 XML 元素 |
| `OutlineElement` | 大纲元素模型，包含类型、内容、样式和嵌套子元素 |
| `OneNoteStyle` | OneNote 样式模型（record 类型），包含字体、颜色、大小等属性 |

**依赖**: MarkdownEngine, ThemeManager

**注意**: `OneNoteApiWrapper` 和 `OneNotePageUpdater` 已移至 OneNoteAddIn 项目，因为它们直接依赖 OneNote COM Interop。

### RenderingServices

代码高亮和 HTML 模板生成服务。

| 类 | 职责 |
|---|------|
| `CodeHighlightService` | 代码语法高亮，内置 17 种语言的词法规则，基于正则分词实现 |
| `HtmlTemplateGenerator` | HTML 模板生成器，为各类渲染生成包含 CDN 脚本的 HTML 页面 |
| `RenderResult` | 渲染结果模型，包含成功/失败状态、图片数据和渲染耗时 |

**依赖**: MarkdownEngine

**注意**: `WebView2Helper`、`DiagramRenderService`、`LatexRenderService` 已移至 OneNoteAddIn 项目，因为它们直接依赖 WebView2 WinForms 控件。

### ImportExport

Markdown 文件导入导出和剪贴板操作。

| 类 | 职责 |
|---|------|
| `MarkdownImporter` | 导入器，支持从文件、剪贴板和文本导入 Markdown 并解析 Front Matter |
| `MarkdownExporter` | 导出器，支持导出到文件、剪贴板以及从 OneNote 页面导出 |
| `FrontMatterParser` | YAML Front Matter 解析器，支持解析和序列化元数据 |
| `ImageHandler` | 图片处理器，支持图片提取、Base64/本地文件互转和远程图片下载 |
| `ClipboardHelper` | Windows 剪贴板操作，支持纯文本和 HTML 格式 |

**依赖**: MarkdownEngine, Markdig (NuGet)

### ThemeManager

CSS 主题管理系统。

| 类 | 职责 |
|---|------|
| `ThemeManager` | 主题管理器，从 CSS 文件加载主题，支持主题列表查询和重新加载 |
| `CssVariableParser` | CSS 变量解析器，从 `:root` 块中提取支持的主题变量 |
| `CssStyleMapper` | CSS → OneNote 样式映射器，将 CSS 变量映射为 OneNote 支持的样式属性 |
| `Theme` | 主题数据模型 |
| `OneNoteStyle` | OneNote 样式模型（ThemeManager 层），包含字体、颜色、行高等属性 |
| `BorderStyle` | 边框样式模型 |

**依赖**: 无外部依赖

## 数据流

### 渲染流程

```
用户输入 Markdown → KeyboardHook 捕获按键 → MarkdownRenderHandler
    → OneNoteApiWrapper 获取当前页面内容
    → MarkdownParser 解析为 MarkdownDocument
    → OneNoteXmlConverter 转换为 OneNote XML 元素
    → ThemeManager 应用主题样式
    → OneNotePageUpdater 更新页面内容
```

### 导出流程

```
F8 按键 → ExportHandler
    → OneNoteApiWrapper 获取页面 XML
    → OneNoteToMarkdownConverter 转换为 Markdown
    → ClipboardHelper 复制到剪贴板
```

### 导入流程

```
Ribbon 按钮点击 → ExportHandler
    → MarkdownImporter 从文件读取 Markdown
    → FrontMatterParser 解析元数据
    → ImageHandler 处理图片
    → OneNotePageUpdater 追加内容到页面
```

## 关键设计决策

1. **.NET Framework 4.8 作为 AddIn 宿主** — .NET 8 + COM Host 方案在 OneNote 中无法正常加载，改用 .NET Framework 4.8 + RegAsm 是最稳妥的方案
2. **业务层使用 .NET Standard 2.0** — .NET Framework 4.8 可以引用 .NET Standard 2.0 项目，业务逻辑代码无需修改
3. **Lazy 延迟初始化** — 所有重型组件（ApiWrapper、RenderHandler、WebView2、KeyboardHook）使用 `Lazy<T>` 延迟到首次使用时才初始化，OnConnection 只做最轻量的日志初始化
4. **ClassInterfaceType.None** — 不使用 AutoDispatch，确保 COM 互操作的安全性
5. **RegAsm 替代 comhost** — 不再使用 .NET 8 的 EnableComHosting/comhost.dll，改用 RegAsm.exe 注册
6. **Markdig 作为解析基础** — 利用 Markdig 成熟的 AST 和管道扩展机制，通过自定义扩展实现数学公式和图表的特殊处理
7. **WebView2 离屏渲染** — 通过隐藏的 WebView2 控件加载 HTML 模板，利用 KaTeX/Mermaid 等 JS 库进行渲染后截图
8. **异步日志** — 使用 `ConcurrentQueue` + `Timer` 实现低开销的异步日志写入，避免阻塞 UI 线程
9. **COM 互操作** — 手动定义 `IDTExtensibility2` 和 `IRibbonExtensibility` 接口，避免依赖 Visual Studio VSTO 工具链
10. **Polyfill 机制** — 通过 `src/Shared/Polyfills.cs` 为 .NET Standard 2.0 提供 `IsExternalInit`、`RequiredMemberAttribute`、`CompilerFeatureRequiredAttribute` 等类型，支持 C# 9+ 的 `init`/`required` 语法
